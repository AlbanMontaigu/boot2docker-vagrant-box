machine:
  pre:
    - wget https://releases.hashicorp.com/packer/0.12.3/packer_0.12.3_linux_amd64.zip -O /tmp/packer.zip && sudo unzip /tmp/packer.zip -d /usr/local/bin
    - sudo apt-get -y update && sudo apt-get -y install m4
dependencies:
  override:
    - make packer-file
test:
  override:
    - make packer-validate